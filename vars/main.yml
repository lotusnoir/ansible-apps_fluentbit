---
# Repo vars
fluentbit_repository_key: "https://packages.fluentbit.io/fluentbit.key"
fluentbit_repository: "deb https://packages.fluentbit.io/{{ ansible_distribution | lower }}/{{ ansible_distribution_release }} {{ ansible_distribution_release }} main"
fluentbit_repository_file: "fluentbit"

# Package vars
fluentbit_tgz: "v{{ fluentbit_version }}.tar.gz"
fluentbit_tgz_url: "https://github.com/fluent/fluent-bit/archive/{{ fluentbit_tgz }}"
fluentbit_pkg_name: "fluent-bit"
